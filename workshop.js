// PsuedoCode
 
// - Create a string for the user using let
// - Assign 3 variables using let, and create a mathematic equation to get the number using a given operator
// - Add comments and use alert to display the vault codes
// - Commit to github and send link 

//




let comb_1 = 5 + 5; //arithmetic for first combination digit
let comb_2 = 80 / 2; //arithmetic for second combination digit
let comb_3 = 6 * 5 + 9; //arithmetic for third combination digit
let userMessage = //message for user to receive
	"You have received this message because you have been chosen to open an important vault. Here is the secret combination:";
alert(`${userMessage}  ${comb_1}-${comb_2}-${comb_3} `); //Alert Dialog Box

